name: remind

x-app: &app
  build:
    context: .
    dockerfile: Dockerfile
    args:
      VERSION: ${VERSION:-dev}
  image: tg-reminder-bot
  environment:
    CONFIG_FILE: /app/config.yaml
    TELEGRAM_TOKEN: ${TELEGRAM_TOKEN:-}
    LLM_API_KEY: ${LLM_API_KEY:-}
    EPG_SERVICE_API_KEY: ${EPG_SERVICE_API_KEY:-}
    EPG_SERVICE_BASE_URL: ${EPG_SERVICE_BASE_URL:-}
    IPTVX_EPG_FILE: ${IPTVX_EPG_FILE:-/data/iptvx_epg.xml.gz}
    ADMIN_API_TOKEN: ${ADMIN_API_TOKEN:-}
    LOG_LEVEL: ${LOG_LEVEL:-}
    DATABASE_DRIVER: ${DATABASE_DRIVER:-sqlite}
    DATABASE_DSN: ${DATABASE_DSN:-/data/remind.db}
    DATABASE_URL: ${DATABASE_URL:-}
  volumes:
    - reminddata:/data
    - ./config.yaml:/app/config.yaml:ro
  restart: unless-stopped

services:
  migrate:
    <<: *app
    command: ["/usr/local/bin/remindctl", "migrate", "up"]
    restart: on-failure

  bot:
    <<: *app
    command: ["/usr/local/bin/bot"]
    depends_on:
      migrate:
        condition: service_completed_successfully

  worker:
    <<: *app
    command: ["/usr/local/bin/worker"]
    depends_on:
      migrate:
        condition: service_completed_successfully

  api:
    <<: *app
    command: ["/usr/local/bin/api"]
    ports:
      - "${API_BIND:-127.0.0.1}:${API_PORT:-8080}:8080"
    depends_on:
      migrate:
        condition: service_completed_successfully

volumes:
  reminddata:
